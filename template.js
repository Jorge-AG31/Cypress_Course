/// <reference types="cypress" />

describe('', () => {
    it("", () => {
        cy.log("");
        cy.visit("");
        
        cy.get("").type("");
        cy.wait();
        
        cy.get("").click();
        cy.wait();

    });
});